{
  "compilerOptions": {
    "target": "ESNext",
    "moduleResolution": "node",
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": [
    "environment.d.ts",
    "src/**/*"
  ],
}